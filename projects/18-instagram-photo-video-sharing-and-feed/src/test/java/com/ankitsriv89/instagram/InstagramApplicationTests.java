package com.ankitsriv89.instagram;

import org.junit.jupiter.api.Test;

/**
 * Placeholder scaffold test. A full @SpringBootTest context-load test is added
 * once the config beans (Redis, Kafka, MinIO) are wired in the build milestones.
 */
class InstagramApplicationTests {

    @Test
    void scaffoldSanity() {
        // Structure compiles; real domain tests arrive with each build milestone.
    }
}
